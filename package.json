{
  "name": "probx-prediction",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "anchor test",
    "test:ts": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "protocol:config": "ts-node -P ./tsconfig.json scripts/protocol_config.ts",
    "market:create": "ts-node -P ./tsconfig.json scripts/create_market.ts",
    "market:buy": "ts-node -P ./tsconfig.json scripts/buy_shares.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "chai": "^4.3.10",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.3"
  }
}
